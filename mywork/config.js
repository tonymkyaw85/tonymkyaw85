/* MyWork configuration.
 * apiUrl: where the PHP API lives, relative to this page ("api/" when the app and API are
 * uploaded together to the NAS). Leave it empty to run the offline demo instead. */
window.MYWORK_CONFIG = {
  apiUrl: "api/",
};
